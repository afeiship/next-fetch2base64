(function () {
  var nx = require('next-js-core2');
  var NxFetch2base64 = require('../src/next-fetch2base64');

  describe('NxFetch2base64.methods', function () {
    test('init', function () {
      var data = {
        key: 1,
        value: 2
      };
      // console.log(rs1, rs2);

      // expect(rs1).toBe(true);

    });
  });
}());
